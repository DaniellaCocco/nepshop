import React from 'react'
import ReactDOM from 'react-dom'
import Shop from './containers/shop'

const e = React.createElement
const root = document.getElementById('root')

ReactDOM.render(e(Shop, null), root)
