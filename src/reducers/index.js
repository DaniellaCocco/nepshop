const initialState = {
  cart: [],
  total_number: 0,
  total_cash: 0
}

const shopReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default shopReducer
